plugins {
    id 'java-library'
    id 'maven-publish'
}

dependencies {
    implementation 'org.springframework.cloud:spring-cloud-starter-openfeign'

    implementation 'com.ztlsir:education-shared-model:0.0.1-20200604.093945-2'
    implementation 'com.ztlsir:education-shared-validation:0.0.1-20200604.093955-1'
    implementation 'com.ztlsir:education-shared-value:0.0.1-20200604.094004-2'
}

jar {
    enabled = true
}

task sourcesJar(type: Jar) {
    from sourceSets.main.allJava
    archiveClassifier = 'sources'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = rootProject.group
            artifactId = rootProject.apiArtifact
            version = rootProject.apiVersion
            from components.java
            artifact sourcesJar{
                classifier "sources"
            }
        }
    }

    repositories {
        maven {
            url = "http://localhost:1001/repository/maven-snapshots/"
            credentials {
                username 'upload'
                password '123456'
            }
        }
    }
}

apply from: "${projectDir}/gradle/version-info.gradle"